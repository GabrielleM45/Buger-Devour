
<div style="background:transparent !important"
class="jumbotron jumbotron-fluid">
  <div class="container">
    <h1 class="display-4">Eat Da Hamberders!</h1>
    <p class="lead">You're hungry and this game won't satisfy your hunger but it may help give you a distraction unitl you can get your burger on. Enter a burger name and when you're ready DEVOUR IT!</p>
  </div>
</div>


<div class ="container-fluid">
<div class="row ">
<div class="col-sm-6">
<h2>Hamberders to be DEVOURED</h2>

<ul class="custom-list">
  {{#each burgers}}
    {{#unless devoured}}

        {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>
</div>


<div class="col-lg-6">
<h2>DEVOURED Hamberders!</h2>

<ul class="custom-list">
  {{#each burgers}}
    {{#if devoured}}

        {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>
</div>
</div>



<div class="row">
<div class="card">
<form class="create-form">
<p>Add a Hamberder to eat!</p>
<div class="input-group mb-3">
  <input type="text" id="newBurger" name="burger_name" class="form-control" placeholder="Hamberder Name" aria-label="Hamberder Name" aria-describedby="button-addon2">

  <div class="input-group-append">
    <button class="btn btn-outline-secondary" type="submit" id="button-addon2">Add a Berder</button>
  </form>
  </div>
  </div>
</div>
</div>

